PROFILE_DESCRIPTION="Staging/Homolog — agressividade alta, dump habilitado"

# sqlmap
SQLMAP_LEVEL=3
SQLMAP_RISK=2
SQLMAP_THREADS=5
SQLMAP_DUMP=true
SQLMAP_MAX_URLS=100
SQLMAP_TIMEOUT_URL=300

# Metasploit
MSF_ENABLED=true
MSF_PAYLOAD="generic/shell_reverse_tcp"
MSF_TIMEOUT=1800

# Brute force (Hydra)
BRUTE_ENABLED=true
BRUTE_TIMEOUT=300
BRUTE_TASKS=8

# Nikto
NIKTO_ENABLED=true
NIKTO_TIMEOUT=700
NIKTO_MIN_SEVERITY=2   # só medium+

# Limites gerais
MAX_EXPLOITS=50
