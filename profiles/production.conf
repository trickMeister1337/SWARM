PROFILE_DESCRIPTION="Produção (janela aprovada) — mínimo impacto, só confirmação"

# sqlmap
SQLMAP_LEVEL=1
SQLMAP_RISK=1
SQLMAP_THREADS=1
SQLMAP_DUMP=false
SQLMAP_MAX_URLS=10
SQLMAP_TIMEOUT_URL=120

# Metasploit
MSF_ENABLED=false
MSF_PAYLOAD="NONE"
MSF_TIMEOUT=600

# Brute force (Hydra)
BRUTE_ENABLED=false
BRUTE_TIMEOUT=120
BRUTE_TASKS=1

# Nikto
NIKTO_ENABLED=false
NIKTO_TIMEOUT=300
NIKTO_MIN_SEVERITY=3   # só high+

# Limites gerais
MAX_EXPLOITS=10
